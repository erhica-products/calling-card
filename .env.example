# Copy to .env and adjust. Never commit your real .env.
PORT=3000

# Where to deliver contact-form submissions. The default handler just logs them.
# Wire this up to email/DB/queue in src/routes/contact.js.
CONTACT_SINK=log
